# cefacdaca.ro — Backend Proxy

## Deploy pe Railway

### Variabile de mediu necesare:
- `ANTHROPIC_API_KEY` — cheia ta de la console.anthropic.com
- `ALLOWED_ORIGIN` — ex: `https://cefacdaca.ro` (sau `*` pentru test)
- `PORT` — setat automat de Railway

### Start:
```
npm install
npm start
```
